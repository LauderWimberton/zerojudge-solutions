#include<iostream>
#include<vector>

using namespace std;

int N, K, R;

void bfs(){

}

//topo
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);

    cin >> R;

    while(R--){
        cin >> K;

        vector<vector<int>> eg(N+1, vector<int>());
        vector<int> dep(N+1 ,0);

    }
}