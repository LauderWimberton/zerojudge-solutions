#include<bits/stdc++.h>

using namespace std;

const int siz = 10000;
pair<int, int> sa[siz];

int n, M, w, p, maxi;

int main(){

    cin >> n >> M;

    for(int i = 0; i < n; i++){
        cin >> w >> p;
        sa[i] = (w, p);
    }

    sort(sa, sa+n);

    for(int i = n; i >= 0; i--){
        
    }

    

}
